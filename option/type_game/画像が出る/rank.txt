// 称号計算関数（おきにいり風）
function getRankTitle(cpm, acc) {
  if (acc < 70) return "うっかり初心者";
  if (cpm >= 250 && acc >= 98) return "伝説のコレクター";
  if (cpm >= 190 && acc >= 95) return "すばやい子馬";
  if (cpm >= 140 && acc >= 90) return "きらめくリボン";
  if (cpm >= 100 && acc >= 85) return "甘いロリポップ";
  if (cpm >= 70) return "シャキシャキりんご";
  if (cpm >= 50) return "お散歩子犬";
  return "ひだまりの子猫";
}

称号の階級イメージ
----------------------------------------
伝説のコレクター（CPM >= 250, 精度 >= 98%）
圧倒的なスピードと正確性を誇るマスター

すばやい子馬（CPM >= 190, 精度 >= 95%）
元気よく駆け抜ける軽やかなタイピング

きらめくリボン（CPM >= 140, 精度 >= 90%）
丁寧でおしゃれにまとまった実力者

甘いロリポップ（CPM >= 100, 精度 >= 85%）
楽しくマイペースに打てる段階

シャキシャキりんご（CPM >= 70）
基本のテンポをつかんできた状態

お散歩子犬（CPM >= 50）
一歩ずつ着実にキーを押せている状態

ひだまりの子猫（上記未満）
のんびり練習をスタートしたところ

うっかり初心者（精度 < 70%）
まずは正確性を意識する段階
